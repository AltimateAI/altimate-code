import { describe, test } from "bun:test"

describe.skip("Color utility", () => {
  // Removed upstream: src/util/color no longer exists in this package.
  test("legacy src/util/color was removed upstream; no replacement API currently exposes this contract", () => {})
})
