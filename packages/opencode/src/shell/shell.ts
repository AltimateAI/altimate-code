// altimate_change start — re-export shim: shell moved to @opencode-ai/core (flat exports); expose
// the Shell namespace the fork callers import.
export * as Shell from "@opencode-ai/core/shell"
// altimate_change end
