You are an INDEPENDENT second-opinion auditor of a large upstream merge (OpenCode v1.4.0 -> v1.17.9) in a fork. Repo /Users/anandgupta/codebase/altimate-code/.claude/worktrees/get_latest_upstream (HEAD). Use bun. READ-ONLY: do NOT modify code/tests, do NOT commit. Your job is to INDEPENDENTLY verify three claims, deriving your own evidence from git + the tree FIRST, THEN compare to the existing reports and flag any disagreement or gap.

Baselines (local): fork pre-merge tip = `main` (merge-base v0.8.9); upstream target = tag `v1.17.9`; prior base = `v1.4.0`.

INDEPENDENTLY VERIFY (derive your own evidence; don't just trust the docs):
1. FORK CHANGES NOT LOST: diff `main`..HEAD. Are there fork-authored files/regions present in main but gone in HEAD that are NOT explained by an upstream removal? Check altimate_change marker deltas, fork dirs (src/altimate, skills, prompts, drivers), and spot-check a few fork features actually resolve in HEAD. Independently sanity-check the claim "0 fork features dropped".
2. UPSTREAM FULLY MERGED: diff `v1.17.9`..HEAD. Any v1.17.9 files/exports missing or stale (present but not at v1.17.9 content) in upstream-owned paths? Independently sanity-check "0 upstream files missing + overlay intact". Pick 3-5 important upstream files (e.g. session/processor, server, provider, tool registry) and confirm HEAD contains v1.17.9's logic (modulo marked fork edits).
3. NOTHING BROKE: run `cd packages/opencode && bun run typecheck` (expect 0) and a representative `bun test` sample across areas (e.g. test/session test/server test/provider test/tool test/upstream — note pass/fail/todo). Independently confirm production runs: SMOKE=$(mktemp -d);cd $SMOKE && timeout 80 bun run --conditions=browser /Users/anandgupta/codebase/altimate-code/.claude/worktrees/get_latest_upstream/packages/opencode/src/index.ts run "say WORKING" --model azure/gpt-4o-mini 2>/dev/null|tail -2.
ALSO REVIEW: the panel consensus in .github/meta/night-run/DECISIONS.md — do you AGREE with the 8 RESTORE / 1 ACCEPT / ~46 TEST-ARCH classification? Spot-check 3-4 deltas against v1.17.9 src yourself. Flag any you'd classify differently (esp. any TEST-ARCH you think is actually a real dropped regression).
FIND THINGS WE MISSED: actively look for merge problems NOT in the existing docs (SHIP-REPORT.md, CONFIDENCE-AUDIT.md, MERGE-REGRESSIONS-FOUND.md) — unmarked behavior changes, dropped fork features outside the obvious dirs, upstream features half-merged, security/branding leaks, etc.

OUTPUT: write your independent findings to /Users/anandgupta/codebase/altimate-code/.claude/worktrees/get_latest_upstream/.github/meta/night-run/CODEX-AUDIT.md with: per-claim INDEPENDENT verdict (agree/disagree + your evidence), any disagreements with DECISIONS.md, and a list of ANY new issues you found that the existing docs missed (or "none found"). Be skeptical and specific.
NOTE: another worker is editing src/session/{processor,prompt,message-v2,compaction}.ts right now — those files are mid-change; note that and judge the rest.
FINAL MESSAGE: your independent verdict on the 3 claims + count of new issues found.
