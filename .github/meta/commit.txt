wip: v1.17.9 reconciliation — opencode TYPECHECK GREEN (0 errors) (checkpoint 32)

The entire packages/opencode typecheck (src + test + the drivers it references) is now GREEN — 0
errors, down from 3181 at the start of this reconciliation. packages/core, packages/tui,
packages/plugin remain green.

This wave: cleared the full ~1222 test-fixture backlog via dual-runtime parallelism — codex (3 rounds:
test/altimate, test/tool+command, and the loose dirs) + ~13 Claude agents (session, control-plane,
server, provider, config, skill, effect, project, release-validation, id, patch, acp). Test fixtures
reconciled to the new APIs: brand re-maps (ProviderV2/ModelV2/Project.ID <-> fork), MessageV2<->SessionV1
shape casts, Effect Service access (X.Service.use / AppRuntime.runPromise), the new Effect Tool API via
a shared test/altimate/tool-fixture.ts (initTool) helper, sync-ified namespace fns, zod()/Schema bridges.
Removed-feature tests .skip-ped with marked notes (ToolRegistry.named, SystemPrompt-as-pure-Service skills
injection, ProviderTransform.smallOptions variants, keybind/color util removals).

Also: restored ConfigPaths.parseText + ProviderAuth.api + Provider.use/Project.use + control-plane/sse.ts
+ MessageV2.latest + Log.init + tool .txt assets (bash.txt) + sync/event.sql; added SystemPrompt Service
facade; added @hono/standard-validator dep (test runtime); drivers/sqlserver Azure optional-peer typed any.

NEXT: actually RUN the suite (bun test) — typecheck-green != tests-pass; runtime failures + the skipped
tests are the remaining work. Then marker-guard --strict cleanup + review/PR.
