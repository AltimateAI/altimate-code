test: [v1.17.9] triage wave 1 — provider/plugin/install/branding/release green (checkpoint 37)

Parallel triage of the 868 runtime test failures (post-bootstrap-fix). This batch (verified
green together: 1336 pass / 36 skip / 2 todo / 0 fail, typecheck 0):
- provider (~160): assertion drift + "No context found" instance-context fixes.
- plugin (~36): ENOENT path + not-a-function API-shape fixes (142 pass/0 fail).
- install + installation + branding + release-validation (~109): content/assertion drift,
  branding-leak src fixes, env-only smoke tests marked .todo/ENV.

Also in-tree (still being finalized by concurrent workers, committed as WIP since edits are
per-file-atomic + typecheck stays 0): session/control-plane DB FK parent-row fixes (main
~300 cluster resolved; smaller compaction/revert/LLM/MessageV2 behavior residuals remain),
and test/tool fixes.

Diagnosed/fixed via parallel codex (local ×3) + Claude agents; remote DGX worker (spark-ec36)
now on wave-2 (command/config/cli/mcp). Each worker scoped to disjoint test dirs. typecheck=0.
