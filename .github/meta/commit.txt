wip: v1.17.9 reconciliation — packages/opencode/src is GREEN (checkpoint 31)

Final src wave (3 parallel agents) cleared the last 52 errors. packages/opencode/src/ now typechecks
with ZERO errors. Session-resume trajectory: opencode 3181 -> 1222 (all remaining are test/ only).

Last fixes: Locale named-imports across cli/cmd/run/*; MessageV2.SYNTHETIC_ATTACHMENT_PROMPT +
Filesystem.resolveFilePath shared helpers; plugin experimental_workspace adapter registry +
config.plugin [spec,options] normalization; SessionShareTable -> @opencode-ai/core/share/sql;
SDK.FileDiff -> SnapshotFileDiff; ProviderV2/ModelV2<->fork brand re-maps at remaining boundaries;
LanguageModelV3 specificationVersion; Effect.scoped + FetchHttpClient for ToolRegistry builtins;
workspace-router-middleware repointed to the Workspace Effect Service; Identifier.create ascending arg.

packages/core + packages/tui + packages/plugin already green. Remaining: ~1222 test errors (many
were src cascades, now stable) — fixture brand-mismatches, MessageV2/SessionV1 shape diffs, and
tests tracking the new Effect Service/Schema APIs. Next: dual-runtime (codex + Claude) test phase.
